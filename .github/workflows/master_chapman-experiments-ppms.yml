# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Python app to Azure Web App - chapman-experiments-ppms

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python version
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Create and start virtual environment
        run: |
          python -m venv venv
          source venv/bin/activate
      
      - name: Install dependencies
        run: pip install -r requirements.txt
        
      # Optional: Add step to run tests here (PyTest, Django test suites, etc.)
      
      - name: Upload artifact for deployment jobs
        uses: actions/upload-artifact@v3
        with:
          name: python-app
          path: |
            . 
            !venv/

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v3
        with:
          name: python-app
          path: .
          
      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v2
        id: deploy-to-webapp
        with:
          app-name: chapman-experiments-ppms
          slot-name: Production
          publish-profile: ${{ secrets.AzureAppService_PublishProfile_a9144ca6329440daa2645e57bd01e7df }}

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@master

#     - name: Set up Python version
#       uses: actions/setup-python@v4
#       with:
#         python-version: '3.11'

#     - name: Build using AppService-Build
#       uses: azure/appservice-build@v3
#       with:
#         platform: python
#         platform-version: '3.11'

#     - name: 'Deploy to Azure Web App'
#       uses: azure/webapps-deploy@v2
#       with:
#         app-name: 'chapman-experiments-ppms'
#         slot-name: 'production'
#         publish-profile: ${{ secrets.AzureAppService_PublishProfile_a9144ca6329440daa2645e57bd01e7df }}